apiVersion: apps/v1
kind: Deployment
metadata:
  name: breaddesk-api
  namespace: openclaw
spec:
  replicas: 1
  selector:
    matchLabels:
      app: breaddesk-api
  template:
    metadata:
      labels:
        app: breaddesk-api
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: api
        image: ghcr.io/botjoonwoo/breaddesk-api:0.1.0
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://breaddesk-postgres:5432/breaddesk
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: breaddesk-secrets
              key: DB_USERNAME
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: breaddesk-secrets
              key: DB_PASSWORD
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: breaddesk-secrets
              key: JWT_SECRET
        - name: BREADDESK_AI_PROVIDER_URL
          value: "http://localhost:11434"
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: breaddesk-api
  namespace: openclaw
spec:
  selector:
    app: breaddesk-api
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
